Django>=4.2,<5.3
djangorestframework>=3.14
mysqlclient>=2.1
# If mysqlclient fails to install, use PyMySQL instead (already wired up in
# config/__init__.py). Just install it with:  pip install pymysql
PyMySQL>=1.1
PyJWT>=2.8
celery>=5.3
kombu>=5.3
# Only rentals-service makes outbound HTTP calls (the one-time bootstrap_caches
# sync against books-service/users-service).
requests>=2.31
python-dotenv>=1.0
